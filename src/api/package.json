{
  "name": "claude-usage-led-api",
  "version": "1.0.0",
  "description": "Node.js HTTP service exposing the Claude five-hour usage window to the LED firmware.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
